---
name: integration-test-plan
description: 集成测试计划——制定服务间集成测试策略、用例和 Mock 方案
argument-hint: "<服务名称或业务流程描述>"
allowed-tools: ["Read", "Write", "Glob", "Bash(mkdir*)", "AskUserQuestion"]
---

# 集成测试计划

用户传入的参数：`$ARGUMENTS`

---

## 加载引用

使用 Read 工具加载以下引用文件，严格遵守其中所有规则：

- `references/integration-test-plan-principles.md` — 集成测试原则（分层策略、数据管理、Mock 策略、用例设计、CI/CD 集成、环境管理）

---

## 强制执行规则

- ✅ 始终用中文与用户沟通
- 🎯 测试用例的断言必须具体——不接受"验证返回正确"
- 🔄 每个业务流程覆盖正向/异常/降级三类场景
- 🧪 每个测试用例必须标注前置条件和清理操作
- ⏸️ 用户确认后才保存，不到确认不进入下一步

---

## 前置条件

确定当前工作目录：检查 `_api-tests/` 下最近创建的日期目录。若无，询问用户任务简写并创建 `_api-tests/{当前日期}-{任务简写}/` 目录结构。

加载以下上下文（如果存在）：
- `{工作目录}/contracts/**` — 契约测试产出
- `{工作目录}/context/**` — API 文档、架构图
- 项目中已有的测试代码和配置
- 当前对话中已有的讨论

---

## Step 1: 梳理集成边界

引导用户明确本次集成测试的范围：

1. **目标服务**：哪个服务是本次测试的主角？
2. **直接依赖**：它直接调用哪些服务/中间件？
3. **间接依赖**：依赖的服务又依赖谁？（画出依赖链路深度）
4. **集成边界线**：哪些依赖用真实服务？哪些用 Mock？

```
[待测服务] --真实--> [数据库]
[待测服务] --真实--> [Redis 缓存]
[待测服务] --Mock--> [支付服务]（第三方）
[待测服务] --真实--> [用户服务]（内部）
[用户服务] --Mock--> [短信服务]（第三方）
```

5. **技术栈确认**：使用什么语言/框架？以此推荐测试工具

**如果已有契约测试产出：**
- 从契约文件中提取服务拓扑
- 向用户确认是否需要调整边界

**⏸️ 等待用户确认后继续。**

## Step 2: 分层测试策略制定

引导用户为三个集成测试层制定策略：

### 2.1 窄集成测试

| 追问 | 目标 |
|------|------|
| "服务直接操作哪些数据库表？核心查询有哪些？" | 确定数据库集成测试范围 |
| "使用了哪些缓存 Key？缓存失效策略是什么？" | 确定缓存集成测试范围 |
| "发送/消费哪些消息？消息格式是什么？" | 确定消息队列集成测试范围 |
| "用 Testcontainers 还是嵌入式中间件？" | 确定技术方案 |

### 2.2 宽集成测试

| 追问 | 目标 |
|------|------|
| "核心业务流程有哪些？（如下单 -> 支付 -> 发货）" | 识别关键路径 |
| "哪些服务参与这个流程？" | 确定测试环境需要的服务 |
| "流程中的数据一致性如何保证？" | 确定一致性验证点 |
| "测试环境怎么部署？Docker Compose 还是 K8s？" | 确定环境方案 |

### 2.3 冒烟测试

| 追问 | 目标 |
|------|------|
| "部署后最关键的 3-5 个接口是什么？" | 确定冒烟测试用例 |
| "什么情况下应该自动回滚？" | 确定回滚触发条件 |

**每层策略制定后向用户展示，确认后再进入下一层。**

## Step 3: 测试用例编写

对每个集成层，按业务流程编写具体用例：

### 用例分类

对每个业务流程，必须覆盖：

1. **正向流程**：核心路径正常执行
2. **异常流程**：
   - 依赖服务返回错误（400/500）
   - 依赖服务超时
   - 依赖服务不可用
3. **降级流程**：
   - 缓存不可用时的降级
   - 非核心依赖不可用时的降级
4. **边界条件**：
   - 空数据 / 超大数据
   - 并发操作 / 竞争条件

### 每个用例必须包含：

```markdown
### TC-[编号]: [场景描述]

**测试层级：** 窄集成 / 宽集成 / 冒烟
**前置条件：** [需要准备的数据和服务状态]
**触发操作：**
  1. [具体的 API 调用或事件触发]
**验证点：**
  - 响应状态码：[具体值]
  - 响应 Body：[关键字段和预期值]
  - 数据库状态：[变更后的数据状态]
  - 事件/消息：[是否发出预期消息]
  - 副作用：[日志、指标、通知]
**清理操作：** [测试后需要回滚/删除的数据]
```

**每编写完一组用例，向用户展示，等待确认和补充。**

## Step 4: Mock 配置方案

对需要 Mock 的依赖，制定 Mock 配置：

1. **工具选型**：根据协议和技术栈推荐 WireMock / MockServer / Prism
2. **Mock 行为定义**：

```markdown
### Mock: [服务名称]

**正常响应：**
- 请求匹配规则：[方法 + 路径 + 参数条件]
- 响应：[状态码 + Body]

**错误响应（用于异常流程测试）：**
- 触发条件：[特定参数值或 Header]
- 响应：[错误状态码 + 错误消息]

**延迟响应（用于超时测试）：**
- 触发条件：[特定参数值]
- 延迟：[毫秒数]

**服务不可用（用于降级测试）：**
- 触发条件：[特定阶段关闭 Mock]
```

3. **Mock 与契约同步**：Mock 行为必须与已有契约一致

## Step 5: 保存产出

将集成测试计划保存到 `{工作目录}/integration/integration-test-plan-{日期}.md`，包含：
- 集成边界图
- 分层测试策略
- 所有测试用例（按层级和业务流程组织）
- Mock 配置方案
- CI/CD 集成建议

## Step 6: 菜单

使用 `AskUserQuestion` 工具向用户展示以下选项（不要用文本菜单让用户输入序号）：

产出摘要：覆盖 [N] 个业务流程，窄集成 [a] 个用例、宽集成 [b] 个用例、冒烟 [c] 个用例。Mock [d] 个外部依赖。

- **进入 API 健康检查（推荐）** — 设计健康检查端点和监控告警方案
- **补充更多测试用例** — 如果还有遗漏的场景
- **回到契约测试** — 如果发现需要补充契约

**⏸️ 停下来等待用户选择。不要自动执行。**

---

## 自检清单

| 维度 | 合格标准 |
|------|---------|
| 分层清晰 | 窄集成/宽集成/冒烟各有对应用例 |
| 场景覆盖 | 每个流程覆盖正向/异常/降级三类 |
| 断言具体 | 无"验证返回正确"式模糊断言 |
| 可重复 | 每个用例有前置条件+清理操作 |
| Mock 一致 | Mock 行为与契约定义一致 |
| 数据隔离 | 并行运行不冲突 |

<IMPORTANT>
本技能完成后，展示菜单并等待用户选择下一步。不要自动执行后续技能。
</IMPORTANT>
