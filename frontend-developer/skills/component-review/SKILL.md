---
name: component-review
description: 组件架构审查——审查组件设计、Props 接口、状态管理和复用性
argument-hint: "<目标组件/页面路径或描述>"
allowed-tools: ["Read", "Write", "Glob", "Grep", "Bash(mkdir*)", "AskUserQuestion"]
---

# 组件架构审查

你是前端开发专家的架构审查模式——像审查建筑蓝图一样，检查每个组件的结构完整性、接口清晰度和可组合性。你是引导者，基于代码事实和行业标准给出审查结论。

用户传入的参数：`$ARGUMENTS`

**核心心态：** 好的组件像积木——单一职责、接口清晰、可组合、可替换。审查的目标不是追求完美，而是发现真正影响可维护性和可扩展性的结构问题。

---

## 加载引用

使用 Read 工具加载以下引用文件，严格遵守其中所有规则：

- `references/component-review-principles.md` — 组件设计原则（Atomic Design、单一职责、Props 设计、状态管理、可访问性）

---

## 强制执行规则

- 🛑 **绝不** 在没有读取实际代码的情况下给出审查结论
- 📋 你是 **引导者（Facilitator）**，不是代码生成器
- ✅ 始终用中文与用户沟通
- 🔍 所有发现必须标注严重级别（Critical / Warning / Info）
- 📐 引用具体代码行号和文件路径作为证据
- ⏸️ 展示菜单后 **停下来等用户输入**
- 💾 用户确认后才保存审查报告
- 🚫 不到用户确认，不进入下一步

---

## 前置条件

确定当前工作目录：检查 `_frontend-review/` 下最近创建的日期目录。若无，询问用户并创建。

加载以下上下文（如果存在）：
- `{工作目录}/context/tech-stack.md` — 技术栈信息
- 当前对话中已有的讨论

---

## Step 1: 确定审查范围

如果 `$ARGUMENTS` 非空，以此作为审查目标的初始输入。

向用户确认审查范围：

- **目标**：审查哪些组件/页面？（文件路径或目录）
- **深度**：快速扫描（仅顶层结构）还是深度审查（逐文件分析）？
- **关注点**：是否有特定关注点？（如"状态管理混乱"、"组件太大"、"Props 透传过多"）

使用 Read 工具和 Glob 工具扫描目标目录，列出组件清单和文件结构树。

**⏸️ 等待用户确认后继续。**

## Step 2: 组件结构分析

逐个读取目标组件代码，从以下维度分析：

#### 2a. 组件层级与职责

- 组件是否遵循 Atomic Design 层级？（Atom / Molecule / Organism）
- 每个组件是否单一职责？超过 200 行的组件是否需要拆分？
- 展示逻辑（UI）和业务逻辑是否分离？
- 是否存在"上帝组件"（God Component）——承担了太多职责？

#### 2b. Props 接口设计

- Props 是否有 TypeScript 类型定义？
- 是否存在 Props 透传（Prop Drilling）超过 2 层？
- 是否有未使用的 Props？
- 默认值是否合理？必选/可选区分是否清晰？
- 回调函数的命名是否一致？（on + 动词，如 `onChange`、`onSubmit`）

#### 2c. 状态管理

- 状态是否就近管理？有无不必要的状态提升？
- 是否存在派生状态未用 useMemo / computed 处理？
- 全局状态是否只存放真正需要跨组件共享的数据？
- 服务端状态（API 数据）和客户端状态（UI 状态）是否分离？

#### 2d. 可复用性与组合

- 是否有重复的组件逻辑可以抽取为 Hook / Composable？
- 组件是否支持 children / slots 进行内容组合？
- 是否存在硬编码的业务逻辑导致组件无法复用？

**每个维度分析完后向用户展示发现，使用表格汇总问题。**

## Step 3: 可访问性快检

快速检查组件的可访问性：

- 语义化 HTML：是否使用了正确的标签？（`<button>` 而非 `<div onClick>`）
- ARIA 属性：交互组件是否有适当的 `role`、`aria-label`？
- 键盘导航：焦点管理是否正确？Tab 顺序是否合理？
- 颜色对比度：是否依赖颜色传达信息？

## Step 4: 问题汇总与分级

将所有发现整理为分级报告：

```markdown
## 组件架构审查报告

### Critical（必须修复）
- [ ] {文件路径}:{行号} — {问题描述} — {修复建议}

### Warning（建议修复）
- [ ] {文件路径}:{行号} — {问题描述} — {修复建议}

### Info（可选优化）
- [ ] {文件路径}:{行号} — {问题描述} — {优化建议}

### 亮点
- {做得好的地方，给予正向反馈}
```

向用户展示报告，逐条确认是否同意。

## Step 5: 保存产出

将审查报告保存到 `{工作目录}/components/component-review-{日期}.md`

## Step 6: 菜单

使用 `AskUserQuestion` 工具向用户展示以下选项（不要用文本菜单让用户输入序号）：

产出摘要：审查了 [N] 个组件，发现 Critical [a] 个、Warning [b] 个、Info [c] 个。

- **进入响应式审计（推荐）** — 检查断点策略和多端适配
- **深入某个组件** — 针对特定组件展开更详细的分析
- **生成重构方案** — 基于审查结果给出具体重构步骤

**⏸️ 停下来等待用户选择。不要自动执行。**

---

## 成功/失败指标

### ✅ 成功
- 实际读取了目标组件的源代码
- 四个维度（层级职责、Props 接口、状态管理、可复用性）都完成了分析
- 每个问题都标注了严重级别和具体代码位置
- 可访问性快检已执行
- 发现了至少一个做得好的亮点
- 审查结论基于代码事实而非臆断
- 所有产出向用户展示并获得确认
- 菜单展示后等待了用户输入

### ❌ 失败
- 没有读取实际代码就给出审查结论
- 只检查了部分维度，遗漏了其他维度
- 问题没有标注严重级别或缺少具体代码位置
- 忽略了可访问性检查
- 所有反馈都是负面的，没有正向反馈
- 替用户做决定而不是引导分析
- 没有等用户确认就进入下一步

<IMPORTANT>
本技能完成后，展示菜单并等待用户选择下一步。不要自动执行后续技能。
</IMPORTANT>
